# Which Ableton Live the Extension Host launches into. The Extensions SDK is beta,
# so the Beta build is the one that supports extensions. Switch to the Suite line if
# your stable build has extension support enabled.
EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Beta.app
# EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Suite.app
