{
  "name": "abletonsdk-mcp-server",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "stdio MCP server proxying typed Ableton/sound-intent tools to the kernel extension inside Live (ws://127.0.0.1:17890).",
  "scripts": {
    "start": "node src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node test/test-mcp.mjs"
  },
  "engines": {
    "node": ">=23.6.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.21.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3"
  }
}
